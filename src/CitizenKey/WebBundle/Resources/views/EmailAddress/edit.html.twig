{% extends "WebBundle:Default:layout.html.twig" %}

{% block title %}Add a new email{% endblock %}

{% block body %}
<h1>Add a new email to {{contact.name}}</h1>

{{form_start(form)}}
{{form_widget(form)}}
{{form_end(form)}}
{% endblock %}
