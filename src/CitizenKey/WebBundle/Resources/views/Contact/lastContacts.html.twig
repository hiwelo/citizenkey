{# Component displaying last created contacts #}

{% if lastContacts %}
<ul>
    {% for contact in lastContacts %}
    <li>
        <a href="{{path('app_contact', { 'contact': contact.id })}}">
            {{contact.name}}
        </a>
    </li>
    {% endfor %}
</ul>
{% endif %}
